
<div class="login-styler">
  <div class="jumbo">
    {{# if loggedOut}}
    <div class="alert alert-danger" role="alert">
      Your have logged out successfully !!
    </div>
    {{/if}}
    <h1 class="display-3"><b>Login</b></h1>
    <h5>Kindly enter your Username and Password details.</h5><br>
    
      {{# unless PassWordError }}
        {{# unless UserNameError }}
          <div class="alert alert-primary alert-dismissible fade show form-signin" role="alert">
            Kindly verify your <strong>Username</strong> and <strong>Password</strong> details before Logging in.
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
        {{ else }}
          <div class="alert alert-danger alert-dismissible fade show form-signin" role="alert">
            {{{ UserNameError }}}
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
        {{/unless}}
      {{ else }}
        <div class="alert alert-danger alert-dismissible fade show form-signin" role="alert">
          {{{ PassWordError }}}
          <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
      {{/unless}}
    <br>
    <div style="padding-right:50">
      <form method="post" class="form-signin" action="/login">

        <div class="input-group mb-3">
          <div class="input-group-prepend">
            <span class="input-group-text" ><b>@</b> </span>
          </div>
          {{# unless UserNameError }}
            <input type="text" class="form-control ctrl" name="username" placeholder="Username" required autofocus>
          {{ else }}
            <input type="text" class="form-control ctrl is-invalid" name="username" placeholder="Username" required autofocus>
            <div class="invalid-feedback">
              Invalid Username. Please enter a valid username.
            </div>

            <!--div class="modal fade in" id="errorModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
              <div class="modal-dialog" role="document">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
                  <div class="modal-body">
                    <b>Privacy Statement for QuadKast</b>
                    <p>&emsp;&emsp;QuadKast has created this privacy statement in order to demonstrate our company’s commitment to privacy. 
                    The following discloses our information gathering and dissemination practices for this website: easyprojects.net</p>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Accept Policy</button>
                  </div>
                </div>
              </div>
            </div-->
          {{/unless}}
        </div>

        <div class="input-group mb-3">
          <div class="input-group-prepend">
            <span class="input-group-text" ><b>P</b></span>
          </div>
          {{# unless PassWordError }}
            <input type="password" class="form-control ctrl" name="password" placeholder="Password" required>
          {{ else }}
            <input type="password" class="form-control ctrl is-invalid" name="password" placeholder="Password" required>
            <div class="invalid-feedback">
              Incorrect Password. Please enter the correct password.
            </div>
          {{/unless}}
        </div>
        <div class="checkbox mb-3">
          <label>
            <input type="checkbox" value="remember-me"> Remember me
          </label>
        </div>
        <div class="row">
          <div class="col" style="text-align:left">
            <button  type="submit" class="btn btn-primary">Login</button>
          </div>
          <div class="col" style="text-align:right;padding-top:10px;">
          <h5 ><a href="/register"> Register now</a></h5>
          </div>
        </div>
      </form>
    </div>
      
    <p class="mt-5 mb-3 text-muted">&copy; 2017-2018</p>
    </div>
  </div>